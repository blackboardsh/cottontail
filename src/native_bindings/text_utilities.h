#ifndef COTTONTAIL_NATIVE_TEXT_UTILITIES_H
#define COTTONTAIL_NATIVE_TEXT_UTILITIES_H

#include <stddef.h>
#include <stdint.h>

intptr_t ct_text_ascii_width(const uint16_t *input, size_t input_len);

#endif
