{
  "name": "cottontail",
  "version": "0.1.1-beta.1",
  "description": "Tiny Zig-based JavaScript runtime scaffold for Electrobun.",
  "license": "MIT",
  "author": "Blackboard Technologies Inc.",
  "type": "module",
  "packageManager": "bun@1.3.10",
  "scripts": {
    "setup": "node scripts/setup.js && node scripts/setup-zig-html-rewriter.js && node scripts/setup-jsc.js",
    "setup:html-rewriter": "node scripts/setup-zig-html-rewriter.js",
    "setup:jsc": "node scripts/setup-jsc.js",
    "pin:jsc": "node scripts/pin-jsc-release.js",
    "check-zig-version": "bun run setup && node scripts/zig.js version",
    "build": "bun run setup && node scripts/zig.js build",
    "build:release": "bun run setup && node scripts/build-release.js",
    "package:release": "node scripts/package-release.js",
    "publish:preview": "node scripts/upload-release-r2.js --all",
    "run": "bun run setup && node scripts/run.js",
    "dev": "bun run run -- test.js",
    "bench:build": "bun run build:release",
    "bench": "bun run bench:build && node scripts/bench.js",
    "compat:surface": "node scripts/generate-api-surface.js",
    "compat:surface:view": "node scripts/show-api-surface.js",
    "compat:surface:all": "bun run compat:surface && bun run compat:surface:view",
    "compat:upstream": "bun run build && node scripts/run-upstream-tests.js all",
    "compat:upstream:list": "node scripts/run-upstream-tests.js all --list",
    "compat:upstream:xfail": "bun run build && node scripts/run-upstream-tests.js all --include-expected-failures",
    "compat:upstream:import": "node scripts/import-upstream-tests.js all",
    "test:zig": "bun run setup && node scripts/zig.js build test",
    "test:js": "bun run build && node scripts/test-js.js",
    "test:electrobun": "bun run build && node scripts/test-electrobun.js",
    "test:electrobun:cli": "bun run build && node scripts/test-electrobun-cli.js",
    "electrobun:window": "bun run run -- electrobun examples/electrobun-window.js",
    "test": "bun run test:zig && bun run test:js",
    "test:blocking": "bun run test && bun run test:electrobun && bun run test:electrobun:cli",
    "release": "bun scripts/tag-release.js",
    "push:beta": "bun run test:blocking && bun scripts/push-version.js beta",
    "push:patch": "bun run test:blocking && bun scripts/push-version.js patch",
    "push:minor": "bun run test:blocking && bun scripts/push-version.js minor",
    "push:major": "bun run test:blocking && bun scripts/push-version.js major",
    "push:stable": "bun run test:blocking && bun scripts/push-version.js stable"
  },
  "devDependencies": {
    "fast-glob": "3.3.1",
    "immutable": "5.1.3",
    "lodash": "4.17.21",
    "uuid": "11.1.0",
    "yaml": "2.9.0"
  }
}
